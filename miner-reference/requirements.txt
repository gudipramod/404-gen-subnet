# Runtime deps for the miner API only. torch / vLLM come from the base image
# (see docker/Dockerfile); the pipeline itself is pure HTTP + Pillow and needs
# neither torch nor transformers.
fastapi==0.115.12
uvicorn[standard]==0.34.3
loguru==0.7.3
requests==2.34.2
pillow==12.2.0
boto3==1.43.45
botocore==1.43.45
pydantic==2.13.4
